Patch Notes (11/18/2019):

New Features:

-Added /percentplotme to percent yourself at a certain percent of a plot's bounds.
-Added sorting options to /currentbet {optional sortby}.
    * 0 or less sorts it by input time.
    * 1 sorts it by name.
    * 2 sorts it by R^2 value.
-Rearranged the y-axis label and title on Triangle plots to be more logical.
-Added /kevinmemorial.

Bug Fixes:

-Fixed a bug where Triangle plots could have error bounds out of the triangle.